{
	"name": "my-app",
	"version": "0.0.1",
	"scripts": {
		"dev": "vite dev --host",
		"build": "vite build",
		"preview": "vite preview --host",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"deploy": "npm run build && node clean.js && gh-pages -d build -t true"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.1.1",
		"@sveltejs/adapter-static": "^3.0.1",
		"@sveltejs/kit": "^2.5.1",
		"@types/cookie": "^0.5.1",
		"gh-pages": "^6.3.0",
		"mdsvex": "^0.11.2",
		"svelte": "^4.2.12",
		"svelte-check": "^3.0.1",
		"tslib": "^2.4.1",
		"typescript": "^5.9.3",
		"vite": "^5.1.4"
	},
	"type": "module"
}
